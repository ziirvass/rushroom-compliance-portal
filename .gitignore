# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/

# Local-only Claude settings (keep out of the public repo)
.claude/settings.local.json

# Node deps (migration tooling installs these locally)
node_modules/

# Never commit secrets / service keys
.env
.env.*

# Supabase CLI local state
supabase/.temp/
supabase/.branches/
