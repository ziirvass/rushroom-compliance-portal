{
  "name": "valcyra-compliance-portal",
  "version": "0.0.0",
  "private": true,
  "description": "VALCYRA / Rushroom Compliance Portal — black-box test harness for the portal-api edge function (Stage 0 of the multi-tenant migration). Runtime is a static frontend + a Supabase edge function; this package adds tests only, no dependencies, no build step.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
